        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
            <div class="container">
                <a class="navbar-brand" href="/">ESGI Crypto</a>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        {% if url is defined and url == "home" %}
                            <a class="nav-item nav-link active" href="/">Accueil<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/">Accueil<span class="sr-only">(current)</span></a>
                        {% endif %}

                        {% if url is defined and url == "md5" %}
                            <a class="nav-item nav-link active" href="/md5">MD5<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/md5">MD5<span class="sr-only">(current)</span></a>
                        {% endif %}

                        {% if url is defined and url == "sha2-256" %}
                            <a class="nav-item nav-link active" href="/sha2-256">SHA2-256<span class="sr-only">(current)</span></a>
	                    {% else %}
                            <a class="nav-item nav-link" href="/sha2-256">SHA2-256<span class="sr-only">(current)</span></a>
		                {% endif %}

	                    {% if url is defined and url == "sha3-512" %}
                            <a class="nav-item nav-link active" href="/sha3-512">SHA3-512<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/sha3-512">SHA3-512<span class="sr-only">(current)</span></a>
                        {% endif %}

                        {% if url is defined and url == "keccak-512" %}
                            <a class="nav-item nav-link active" href="/Keccak-512">Keccak-512<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/Keccak-512">Keccak-512<span class="sr-only">(current)</span></a>
                        {% endif %}

                        {% if url is defined and url == "ripemd160" %}
                            <a class="nav-item nav-link active" href="/RipeMD160">RipeMD160<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/RipeMD160">RipeMD160<span class="sr-only">(current)</span></a>
                        {% endif %}

                        {% if url is defined and url == "aes" %}
                            <a class="nav-item nav-link active" href="/AES">AES<span class="sr-only">(current)</span></a>
                        {% else %}
						    <a class="nav-item nav-link" href="/AES">AES<span class="sr-only">(current)</span></a>
						{% endif %}

					    {% if url is defined and url == "rsa" %}
						    <a class="nav-item nav-link active" href="/RSA">RSA<span class="sr-only">(current)</span></a>
						{% else %}
							<a class="nav-item nav-link" href="/RSA">RSA<span class="sr-only">(current)</span></a>
                        {% endif %}

						{% if url is defined and url == "blowfish" %}
							<a class="nav-item nav-link active" href="/BlowFish">BlowFish<span class="sr-only">(current)</span></a>
                        {% else %}
                            <a class="nav-item nav-link" href="/BlowFish">BlowFish<span class="sr-only">(current)</span></a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>